spring:
  cloud:
    nacos:
      discovery:
        server-addr: http://nacos.chachacha.top:80
      config:
        server-addr: http://nacos.chachacha.top:80
        file-extension: yaml
logging:
  level:
    root: info