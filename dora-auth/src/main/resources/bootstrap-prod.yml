spring:
  cloud:
    nacos:
      discovery:
        server-addr: http://nacos-registry:8848
        service: ${spring.application.name}
      config:
        server-addr: http://nacos-registry:8848
        file-extension: yaml
logging:
  level:
    root: info